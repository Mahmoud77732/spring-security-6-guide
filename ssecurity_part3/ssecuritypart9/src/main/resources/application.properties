# ${varName : defaultValue}  -> environment variable
spring.application.name= ${SPRING_APP_NAME:securitypart1}

logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:TRACE}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3307/springsecurity_db?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.format_sql = true
spring.jpa.hibernate.show_sql = true
spring.jpa.hibernate.ddl-auto = update

spring.config.import = application_prod.properties
# default | prod
spring.profiles.active = prod

###
# (1) create many profiles
#   (1.1) create "application_prod.properties" file
# (2) add properties in "application.properties"
#   (2.1) "spring.config.import"
#   (2.2) "spring.profiles.active"
# (3) add properties in "application_prod.properties"
#   (3.1) "spring.config.activate.on-profile = prod"
# (4) add Environment Variables "VSCode"
#   Environment Variables have higher priority than app.properties
#   (4.1) press "Ctrl+Shift+P"
#   (4.2) Choose "Debug: Open launch.json"
#   (4.3) Add/edit "env" block inside your launch configuration
#       "env": {
#           "SPRING_PROFILES_ACTIVE": "default"
#       }
# (5) create custom "AuthenticationProvider" for "prod" profile
#   (5.1) create the custom "AuthenticationProvider" class
#   (5.2) add "@Profile("prod")" annotation
#   (5.3) add "@Profile("!prod")" annotation on the another classes
#   (5.4) refactor "authenticate()"
# (6) create "ProjectSecurityConfig" for "prod" profile
#   (6.1) create "ProjectSecurityProdConfig" class
#   (6.2) add "@Profile("prod")" annotation
#   (6.3) add "@Profile("!prod")" annotation on the another classes
###


