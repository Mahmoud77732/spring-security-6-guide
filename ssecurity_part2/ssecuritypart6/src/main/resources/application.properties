# ${varName : defaultValue}  -> environment variable
spring.application.name= ${SPRING_APP_NAME:securitypart1}

#spring.security.user.name=${SECURITY_USERNAME:admin}
#spring.security.user.password=${SECURITY_PASSWORD:admin123}

logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:TRACE}


spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3307/springsecurity_db?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.format_sql = true
spring.jpa.hibernate.show_sql = true
spring.jpa.hibernate.ddl-auto = update


###
# (1) DB Config:
#   (1.1) create "customer" table
#    ```SQL
#       create table `springsecurity_db`.`customer`(
#           id int not null AUTO_INCREMENT primary key,
#           email varchar(45) not null,
#           pwd varchar(200) not null,
#           role varchar(45) not null
#       );
#    ```
#   (1.2) insert some users/customers
#    ```SQL
#       INSERT INTO `springsecurity_db`.`customer` 
#           (email, pwd, role) 
#           VALUES('user1@gmail.com', '{noop}User_12345@@', 'read');
#           
#       INSERT INTO `springsecurity_db`.`customer` 
#           (email, pwd, role) 
#           VALUES('admin1@gmail.com', '{bcrypt}$2a$12$UrZPFAis5fQB0fnX16Mm5OM76lXX2IjXTXX/4ZxlKiydgH1JJk6yK', 'admin');
#    ```
# (2) create "Customer" class in "model" package
# (3) create "CustomerRepo" interface in "repo" package
# (4) create _your custom_ "MyUserDetailsService" class
# (5) remove the "@Bean UserDetailsService"
# (6) Testing; now u can use the custom table "customer"
###


