# ${varName : defaultValue}  -> environment variable
spring.application.name= ${SPRING_APP_NAME:ssecurity_oauth2_keycloak_26}

logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:TRACE}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3307/springsecurity_db?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.format_sql = true
spring.jpa.hibernate.show_sql = true
spring.jpa.hibernate.ddl-auto = update

spring.config.import = application_prod.properties

# default | prod
spring.profiles.active = default

server.servlet.session.timeout = ${SESSION_TIMEOUT:20m}

logging.level.org.springframework.security.web.FilterChainProxy = debug

# Keycloak ->> OpenID endpoint link ->> "jwks_uri"
spring.security.oauth2.resourceserver.jwt.jwk-set-uri = http://localhost:8180/realms/hegazyrealm1/protocol/openid-connect/certs

##############################################
#
#TODO Steps (1) :-
#---
#1. create "KeycloakRoleConverter" class in "config" package
#2. refactor security-config-class:
#   2.1. create instance of "JwtAuthenticationConverter" and inject your "KeycloakRoleConverter"
#   2.2. comment "http.httpBasic()", "http.formLogin()"
#   2.3. add "http.oauth2ResourceServer()"
#3. refactor "@GetMapping("/myAccount")" in "AccountController" class
#4. refactor all methods in other controllers in the same way
#5. add props in "application.properties"
#   5.2. http://localhost:8180 ->> hegazyrealm1 ->> Current realm ->> Realm settings ->> endpoints ->> first-link(OpenID endpoint) ->> "jwks_uri"
#   5.1. " spring.security.oauth2.resourceserver.jwt.jwk-set-uri = "jwks_uri"'s value "
#6. 
#
#
#
##############################################

